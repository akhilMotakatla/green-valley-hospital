# Cycle: Krishna Requirements Batch — 2026-07-20

**Opened by**: Krishna (client / product owner)
**Date**: 2026-07-20
**Purpose**: First live requirements batch through the full phase-gate pipeline. 12 requirements covering clinical workflow, patient self-service, analytics, public site UX, and B2B growth.

---

## Batch Summary (12 requirements)

1. Doctor Availability & Slot Management
2. In-App Notification Center
3. Patient Pre-Visit Intake Form
4. Vitals Trend Visualization
5. Inter-Department Referral Management
6. Advanced Analytics & Reporting Dashboard (Admin)
7. Public Symptom / Condition Search
8. Patient Medical Record Export (PDF)
9. Appointment Waitlist System
10. Discharge Summary & Follow-Up Scheduling
11. Patient Satisfaction Survey & Doctor Ratings
12. Corporate Health Check Packages (B2B Revenue)

Full requirement detail is in `docs/krishna-daily-requirements-log.md` (2026-07-20 entry) and in Krishna's response to the user below.

---

## Agent Activity Log

*(Each agent appends their before/during/after entries below as they work through the batch.)*

**Krishna** — Requirements batch delivered to Lavanya for Phase 1 analysis. Awaiting collaborative analysis (Akhil + Sagar + Lavanya + Sunny) before any implementation begins.

## Lavanya — Phase 1 Collaborative Analysis + Phase 2 Documentation — 2026-07-20

**Before work starts**
- Task: Lead Phase 1 analysis of all 12 requirements (batch 2026-07-20), then own Phase 2 documentation in docs/requirements.md.
- Plan: Read existing requirements.md, db/schema.sql, frontend pages list, backend routers to establish baseline. Confirm each requirement is new. Flag ambiguities requiring Krishna clarification. Identify sequencing constraints and inter-requirement dependencies. Consolidate into Phase 2 documentation as Section 9.
- Dependencies relied on: docs/requirements.md (v1.3), db/schema.sql (current), project-status.md. Sagar and Sunny have NOT yet provided their technical/delivery-feasibility input for this batch. Per protocol I am flagging this: design (Phase 3/4) must NOT start until Sagar provides a technical-feasibility pass on the ambiguities listed in the analysis below, particularly around PDF generation library choice (Req 8), the vitals schema gap (Req 4), the appointment-suggestion mechanism for referrals (Req 5), and the survey-trigger mechanism (Req 11) given the absence of a job scheduler in the current stack.

**During work — Phase 1 analysis complete, Phase 2 in progress**
- Confirmed all 12 requirements are genuinely new (no overlap with Sections 1–8 of requirements.md).
- Discovered a pre-existing schema gap: STF-4 (record vitals) has been in scope since Section 2.4, but there is no `vitals` table in db/schema.sql. Req 4 (Vitals Trend Visualization) forces this gap to be addressed. Flagging to Sagar.
- Two items previously listed as Out of Scope in Section 4 are now moving in scope: "Waitlist management for fully booked doctors" (Req 9) and "Data export/interoperability tools (CSV/PDF export of records)" (Req 8 is a targeted, patient-only, on-demand PDF — not a general export tool). Updating Section 4 accordingly.
- The existing `email_notifications` table (Section 8.3) is NOT the in-app notification center. Req 2 introduces a separate `notifications` table for in-app use. These are distinct channels.
- Critical ambiguity on Req 11 (survey trigger): the stack has no background job scheduler. "Triggered 24h after Completed" must be clarified — either a deferred record approach (record created at completion with trigger_after timestamp, polled on login) or a proper async job. Sagar must decide before design.
- Ambiguity on Req 5 (referral acceptance creates "pre-filled appointment suggestion"): unclear whether this auto-creates a Scheduled appointment or merely pre-populates the booking form. Left as open item — Sagar and Krishna must align before design.

**After work completes**
- Files changed: docs/requirements.md (Section 9 added, Section 4 updated), docs/project-status.md (changelog entry added), communication/2026-07-20_00-00-00.md (this entry).
- What remains: Sagar's Phase 3 (UX design) + Phase 4 (technical design) for all 12 requirements. Sagar needs to address the 18 open items listed in Section 9.18 before design is locked.
- Handoff to Sagar: the full requirements are in Section 9 of docs/requirements.md. The 18 open items in Section 9.18 are the priority inputs Sagar needs to resolve or escalate to Krishna. Pay particular attention to: (a) vitals schema gap, (b) PDF library choice, (c) referral appointment-suggestion mechanism, (d) survey trigger mechanism, (e) availability config admin model.

---

## Sagar — Phase 3 UX Design + Phase 4 Technical Design — 2026-07-20

**Before work starts**
- Task: Produce Phase 3 (Product/UX Design) and Phase 4 (Technical Design) for all 12 REQs (REQ-01 through REQ-12), plus resolve all open items assignable to Sagar and document assumptions for Krishna-directed items.
- Plan: Read docs/requirements.md Section 9 in full (all requirements, entities, RBAC, OIs). Read existing db/schema.sql, App.tsx routing, backend router structure, and project-status.md to understand the baseline. Produce docs/technical-design.md with: per-requirement UX journeys, portal assignments, navigation changes, UI states; architectural decisions for OI-2/3/4/8/13; documented assumptions for OI-1/5/6/7/9-12/14-18; full DDL for 16 new tables; API endpoint contract for new sections 11-22; backend service logic for complex flows; frontend component structure and route additions.
- Dependencies: docs/requirements.md Section 9 (Lavanya's Phase 2 — complete). Existing schema.sql and frontend file structure (both read). No blockers.

**During work**
- Read all of Section 9 including all 12 requirements, Section 9.14 (vitals gap), 9.15 (entity definitions), 9.16 (RBAC additions), 9.17 (cross-cutting ACs), 9.18 (18 open items).
- Key decisions made: OI-2 = poll-on-login (no scheduler); OI-3 = WeasyPrint; OI-4 = Recharts; OI-8 = atomic discharge+followup; OI-13 = unique index sufficient.
- Key assumption made that deviates from requirements doc: OI-5 (referral decline) — assumed "stays Declined" not "returns to Pending" per REFFR-4 — flagged for Krishna confirmation. OI-12 (waitlist scope) — assumed per-doctor-per-date per task instructions, deviates from WLFR-1 — flagged.
- Noted additional design decision: `waitlist_entries` needs a `held_slot_time TEXT` column (not in Section 9.15) to support slot exclusion from the public slot query while a notified patient's confirmation window is open. Added this column to the DDL.
- Notified: `notification_schedules.survey_id` is a soft FK (satisfaction_surveys not yet created at DDL parse time in SQLite if defined before it). Documented workaround in schema comments.

**After work completes**
- Files created/changed:
  - `docs/technical-design.md` — created (new file): Phase 3 UX journeys + Phase 4 technical design for all 12 requirements.
  - `db/schema.sql` — appended Batch 2 DDL (16 new tables, `invoices.paid_at` column inline + index).
  - `docs/project-status.md` — changelog entry added.
  - `communication/2026-07-20_00-00-00.md` — this entry appended.
- What remains / handoffs needed:
  - Lavanya: Phase 5 task breakdown. Use §4.5 dependency sequencing (Groups A–E) as the basis for milestone ordering. Group A (schema + slot engine + booking update) must come before Groups B–D.
  - Chintu: Before starting, add new API endpoint sections (11–22) from `docs/technical-design.md §4.2` to `docs/api-spec.md`, update `docs/architecture.md` ER diagram, add `weasyprint>=60.0` to `requirements.txt`, add `recharts` to `package.json`. Note the `waitlist_entries.held_slot_time` column (nullable TEXT) must also be added — it's in the schema.sql DDL already.
  - Krishna confirmation needed before implementing: OI-1 (who configures availability), OI-5 (referral decline behavior), OI-6 (admin sees referral reason?), OI-7 (revenue reporting accuracy), OI-9 (doctor ratings public?), OI-10 (corporate nav placement), OI-11 (contact notification recipients), OI-12 (waitlist scope), OI-14 (date picker presets), OI-15 (search bar placement), OI-16 (intake form cutoff), OI-17 (corporate inquiry notification), OI-18 (public doctor rating).
- Known issues / notes: The discharge summary POST handler and the existing appointment-status-update endpoint BOTH need to trigger survey row creation on Completed status. I've documented this clearly in §4.2 endpoint 20 note and §4.6 item 4. Chintu must not miss the second trigger point.

---

## Lavanya — Phase 5 Task Breakdown & Delivery Plan — 2026-07-20

**Before work starts**
- Task: Produce the Phase 5 delivery plan for all 12 requirements in the 2026-07-20 batch, using Sagar's dependency groups from `docs/technical-design.md` §4.5 as the sequencing basis.
- Plan: Read docs/technical-design.md (all sections, especially §1.1 assumptions, §4.1 schema, §4.2 API endpoints, §4.4 frontend components, §4.5 sequencing), docs/project-status.md, docs/agent-collaboration-protocol.md, and communication/2026-07-20_00-00-00.md to confirm Sagar's Phase 4 is complete and no open blockers remain. Then produce docs/delivery-plan.md with all 30 tasks, milestone dates, and risk register.
- Dependencies: docs/technical-design.md (Sagar Phase 4 — confirmed complete per changelog and this comm log). db/schema.sql (read to verify Batch 2 DDL already applied by Sagar). docs/requirements.md §9 (Phase 2 — confirmed complete). No blockers.

**During work**
- Read all of docs/technical-design.md including §1.1 (13 assumptions), §4.1 (16 new tables + invoices.paid_at), §4.2 (endpoints 11–22), §4.3 (complex service logic), §4.4 (frontend components), §4.5 (dependency sequencing). Read project-status.md and agent-collaboration-protocol.md for context and reporting rules.
- Reconciled task prompt's dependency groupings (Group A=REQ-01+REQ-02, Group B=REQ-09/10/11, Group C=REQ-03/04/05/07/08/12, Group D=REQ-06) with Sagar's §4.5 sequencing to produce a unified 30-task list. The two groupings aligned after reconciliation; primary difference is that REQ-02 notifications backend (Sagar's Group B) is folded into Group A because it is a foundation dependency for REQ-05 referrals in Group C.
- Added 3 pre-implementation housekeeping tasks (TASK-001 to TASK-003: api-spec.md update, architecture.md update, schema/seed/package verification) that must precede all Group A work. These are Chintu's first actions.
- Noted WeasyPrint system-dependency risk (R-1) — Chintu must verify pip install + test write_pdf() call as first step of TASK-025, not after writing application code.
- Milestone plan is realistic (not rubber-stamped): 9 weeks of Chintu implementation spread across 4 groups, plus review/QA overhead per group, with final acceptance target 2026-10-14.

**After work completes**
- Files created/changed:
  - `docs/delivery-plan.md` — created (new file): 30 tasks (TASK-001 through TASK-030), per-task: description, responsible agent, effort (S/M/L/XL), dependencies, milestone/group, definition of done, estimated completion date; group milestones with dates; risk register for all 13 §1.1 assumptions plus 6 additional delivery risks.
  - `docs/project-status.md` — changelog entry added.
  - `communication/2026-07-20_00-00-00.md` — this entry appended.
- What remains / handoffs:
  - **Chintu**: begin immediately with TASK-001 (update docs/api-spec.md), then TASK-002 (docs/architecture.md), then TASK-003 (schema/seed/package verification), then Group A starting with TASK-004 (REQ-01 availability backend). Full task details including definitions of done are in `docs/delivery-plan.md`.
  - **Sunny**: pick up `docs/delivery-plan.md` §3 (Milestone Plan) as the baseline for daily standup tracking and `docs/scrum-tracker.xlsx` updates. All milestone dates are in §3. Do not re-derive task estimates — use the delivery plan as written.
  - **Sagar**: code review (Phase 7) is expected per group. TASK-004 and TASK-011 are the most complex backend tasks — pairing availability noted in the plan. Your review queue starts when Chintu completes Group A (target 2026-08-05).
  - **Gopal**: QA (Phase 8) is per group, starting with Group A after Sagar's review passes. Acceptance criteria are in `docs/requirements.md` §9. Note R-5 (historical invoices will have paid_at=NULL — not a bug).
  - **Krishna**: 13 outstanding assumptions need confirmation before their respective task start dates. Deadline schedule is in `docs/delivery-plan.md` §4.1. The most urgent is OI-1 (who configures availability) — needed before Chintu starts TASK-004 (2026-07-22). If Krishna cannot confirm by 2026-07-22, Akhil should pause TASK-004 and escalate.
- Known issues: All 13 §1.1 assumptions are outstanding — none have been confirmed by Krishna yet as of this writing. This is the primary risk to the schedule. Implementation cannot safely begin on affected tasks without Krishna's input.

---

## Chintu — Group A Foundation Tasks (TASK-001 through TASK-010) — 2026-07-21

**Before work started**
- Task: Implement all 10 Group A foundation tasks from `docs/delivery-plan.md` on branch `feature/chintu-group-a-foundation`.
- Plan: TASK-001 (api-spec.md) → TASK-002 (architecture.md) → TASK-003 (schema/seed/deps) → TASK-004 (availability backend) → TASK-005 (booking slot validation) → TASK-006 (paid_at) → TASK-007 (notification fan-out) → TASK-008 (availability UI) → TASK-009 (SlotPicker) → TASK-010 (notifications UI). Commit after each task.
- Dependencies: Sagar's Phase 4 technical design in `docs/technical-design.md` (complete). Lavanya's Phase 5 delivery plan in `docs/delivery-plan.md` (complete). Sagar's Batch 2 DDL already in `db/schema.sql`.

**During work**
- TASK-001: Appended 12 new endpoint sections (11–22) to `docs/api-spec.md`.
- TASK-002: Updated `docs/architecture.md` with Batch 2 ER additions and library notes.
- TASK-003: Verified 16-table Batch 2 DDL in schema.sql. Added `system_config` and `corporate_packages` seed rows. Added weasyprint and recharts dependencies.
- TASK-004: Created `DoctorAvailabilitySchedule`, `DoctorSlotConfig`, `DoctorAvailabilityBlock` models. Implemented `get_available_slots()` service. Built 19 endpoints across three sub-routers (slots_router public, doctor_avail_router, admin_avail_router). Registered in main.py.
- TASK-005: Updated `POST /api/appointments` to validate requested time against `get_available_slots()`, reject if slot unavailable when availability is configured. Auto-creates pending intake_forms row on booking.
- TASK-006: Added `paid_at` server-side auto-set in `PATCH /invoices/{id}` on Paid transition (OI-7). Added to `_resolve_invoice()` response.
- TASK-007: Implemented `create_notifications()` batch-insert helper and `create_survey_for_appointment()` in notification_service.py. Wired fan-out into billing (invoice_created, invoice_status_changed), doctor (appointment_cancelled, lab_order_assigned, survey on Completed), patient (appointment_cancelled), staff (appointment_cancelled), lab (lab_result_finalized, lab_result_amended), public (contact_form_received → all Admin+Staff), admin (account_created, account_deactivated).
- TASK-008: Built `availability.ts` API client, `AvailabilityWeekGrid.tsx` component, `DoctorAvailabilityPage.tsx` (/doctor/availability), `AdminDoctorAvailabilityPage.tsx` (/admin/users/:userId/availability — resolves doctor_id from user_id via getUser() call). Added Schedule nav item to Doctor sidebar. Added Schedule link button on Doctor rows in AdminUsersPage.
- TASK-009: Built `SlotPicker.tsx` (ARIA role=listbox/option, loading skeleton, empty state). Updated `BookAppointmentPage.tsx` to use REQ-01 slot API + SlotPicker. Updated `StaffAppointmentsPage.tsx` with date+SlotPicker; falls back to manual time input when no slots configured (front-desk override).
- TASK-010: Built `notifications.ts` API client, `NotificationBell.tsx` (60s poll, badge, panel toggle), `NotificationPanel.tsx` (top-10 dropdown, relative time, mark-one/all, "See all" link), `NotificationsPage.tsx` (/notifications, paginated, mark-read). Replaced AppShell placeholder Bell with NotificationBell. Added /notifications route to all 6 role groups in App.tsx.

**After work completes**
- Files changed (key): `docs/api-spec.md`, `docs/architecture.md`, `db/seed/seed.py`, `requirements.txt`, `package.json`, `src/backend/app/models.py`, `src/backend/app/services/availability_service.py`, `src/backend/app/services/notification_service.py`, `src/backend/app/routers/availability.py` (new), `src/backend/app/routers/billing.py`, `src/backend/app/routers/doctor.py`, `src/backend/app/routers/patient.py`, `src/backend/app/routers/staff.py`, `src/backend/app/routers/lab.py`, `src/backend/app/routers/public.py`, `src/backend/app/routers/admin.py`, `src/backend/app/main.py`, `src/frontend/src/api/availability.ts` (new), `src/frontend/src/api/notifications.ts` (new), `src/frontend/src/components/AvailabilityWeekGrid.tsx` (new), `src/frontend/src/components/SlotPicker.tsx` (new), `src/frontend/src/components/NotificationBell.tsx` (new), `src/frontend/src/components/NotificationPanel.tsx` (new), `src/frontend/src/pages/doctor/DoctorAvailabilityPage.tsx` (new), `src/frontend/src/pages/admin/AdminDoctorAvailabilityPage.tsx` (new), `src/frontend/src/pages/shared/NotificationsPage.tsx` (new), `src/frontend/src/pages/patient/BookAppointmentPage.tsx`, `src/frontend/src/pages/staff/StaffAppointmentsPage.tsx`, `src/frontend/src/pages/admin/AdminUsersPage.tsx`, `src/frontend/src/layouts/AppShell.tsx`, `src/frontend/src/App.tsx`, `src/frontend/src/index.css`, `docs/project-status.md`.
- Branch: `feature/chintu-group-a-foundation` — 10 commits, pushed to origin.
- PR: https://github.com/akhilMotakatla/green-valley-hospital/pull/1
- What remains: Sagar Phase 7 code review of PR #1. Once approved and merged by Sagar, Gopal runs Group A QA.
- Handoff to Sagar: PR #1 is ready for your review. Key areas to look at: (1) `get_available_slots()` edge cases (no schedule configured = empty list = staff can book any time), (2) `paid_at` is server-set only — confirm it's not accepted from request body, (3) notification fan-out is inside the same DB transaction as the triggering write, (4) `create_survey_for_appointment()` checks for existing row before insert (idempotent), (5) `AdminDoctorAvailabilityPage` calls `getUser()` to resolve doctor_id — this adds one extra API call per page load.
- Assumptions applied from §1.1: OI-1 (availability configurable by both Doctor and Admin — implemented both sets of endpoints), OI-2 (poll-on-login survey deferred via notification_schedules), OI-7 (paid_at server-set, not accepted from client), OI-11 (contact_form_received goes to all active Admin+Staff).

---

## Sagar — Phase 7 Code Review — PR #1 Group A Foundation — 2026-07-21

**Before work starts**
- Task: Phase 7 code review of PR #1 (`feature/chintu-group-a-foundation`) covering TASK-001 through TASK-010 (REQ-01 Availability, REQ-02 Notifications, OI-7 paid_at).
- Plan: Read project-status.md, docs/technical-design.md (§1.0 OI-2, §4.1–4.4, §4.3.1 fan-out table), docs/requirements.md §9.1–9.2 acceptance criteria. Inspect PR diff (gh pr diff 1), then read all changed implementation files directly. Check against the five flagged areas plus perform full spec-conformance review.
- Artifacts relied on: docs/technical-design.md (Phase 4 — my own design), docs/requirements.md §9 (Phase 2), docs/api-spec.md §11–12 (TASK-001 update).

**During work**
- Read project-status.md, agent-collaboration-protocol.md, docs/technical-design.md (full), docs/requirements.md §9.1–9.2.
- Listed all 35 changed files via gh pr diff. Read key implementation files: availability.py service, notification_service.py, billing.py, notifications.py router, availability.py router, doctor.py router, staff.py router, patient.py (appointment creation path), lab.py (event types), AdminDoctorAvailabilityPage.tsx, NotificationBell.tsx, NotificationPanel.tsx.
- Read schemas.py to confirm BillingInvoicePatchRequest has no paid_at field.

**Findings — Five flagged areas:**
1. `get_available_slots()` front-desk fallback — PASS. Both patient.py and staff.py use `if available_slots and slot_str not in available_slots`. Falsy empty list correctly skips validation, enabling front-desk manual time override.
2. `paid_at` server-only enforcement — PASS. BillingInvoicePatchRequest schema has no paid_at field. Only billing.py handler sets it on Paid transition.
3. Fan-out transaction atomicity — PASS. All create_notifications() calls across all routers precede db.commit() in the same transaction.
4. create_survey_for_appointment() idempotency — PASS. Function queries for existing SatisfactionSurvey row before inserting; returns early if found. Called from both discharge-summary POST and PATCH status→Completed path.
5. AdminDoctorAvailabilityPage getUser() call — PASS. Not redundant. Route uses userId, not doctor_id; getUser() is the only way to resolve userId→doctor_id. Single call per page load.

**Findings — Additional issues (blocking):**

**ISSUE 1 (CRITICAL)**: `check_and_fire_deferred_notifications` in notification_service.py, lines 68–102 — pending_schedules query has no patient_id filter. Loads ALL unfired appointment_reminder schedules globally. `sched.is_fired = 1` is set unconditionally (outside the patient ownership check), so Patient A logging in silently discards Patient B's reminder (marks is_fired=1 without sending). Data-loss bug. Fix: add `Appointment.patient_id == patient.patient_id` filter to query. Design spec OI-2 pseudocode explicitly has this filter.

**ISSUE 2 (MEDIUM)**: No `appointment_noshow` fan-out in doctor.py or staff.py. Requirements §9.2 event table and §4.3.1 both require this event when appointment status → 'NoShow'. Fix: add elif payload.status == 'NoShow' block firing to patient's user_id.

**ISSUE 3 (MEDIUM)**: `lab_result_finalized` used as event_type in lab.py for all 4 notification inserts (initial + amended). Spec (requirements §9.2 line 1904 and §4.3.1) requires `lab_result_ready`. Fix: rename initial finalization events to `lab_result_ready`. For amended path, either use `lab_result_ready` or use a distinct `lab_result_amended` and document it in api-spec.md §12.

**ISSUE 4 (MEDIUM)**: `appointment_reminder` deferred notification only fires for Patient (line 84 guard). Requirements §9.2 and §4.3.1 both say it fires for "Patient (owner), Doctor (assigned)". When a Doctor logs in, no reminder is ever created for them. Fix: add a doctor-scoped path after the patient path, querying schedules for the calling doctor's assigned appointments.

**ISSUE 5 (MINOR)**: NotificationPanel.tsx line 43 uses `page_size: 10`. NOTIFFR-4 (requirements §9.2) and §3.2 of technical-design.md both say the panel shows at most 20 items. Fix: change to `page_size: 20`.

**After work completes (preliminary — fixes pending)**
- PR review comment posted: https://github.com/akhilMotakatla/green-valley-hospital/pull/1#issuecomment-5032169696 (GitHub API prevented request-changes on own-account PR; full findings posted as comment with same detail).
- PR outcome: **BLOCKED from merge** pending fixes to Issues 1–5. Issue 1 is a data-loss class bug that must be addressed before any merge. Issues 2–4 are blocking for REQ-02 acceptance criteria AC-NOTIF-4 (lab result) and the NoShow notification coverage. Issue 5 is minor but should be fixed in the same pass.
- What remains: Chintu fixes Issues 1–5 on the branch, pushes update. Sagar re-reviews. On re-review pass, Sagar approves and merges. Gopal then runs Group A QA.
- Handoff to Chintu: see the 5 issues in PR comment at the URL above. Issue 1 is the most critical — fix the pending_schedules query filter and move sched.is_fired=1 to only run when the notification was actually sent or when the appointment belongs to the current user.
- Note for Gopal (Phase 8): once Sagar re-approves, AC-NOTIF-4 (lab result notifications) should be tested with the new `lab_result_ready` event_type, not `lab_result_finalized`. Confirm the event_type string in the database after fix.

---

## Chintu — PR #1 Review Fixes (Issues 1–5) — 2026-07-21

**Before work starts**
- Task: Fix all 5 issues raised by Sagar in PR #1 review comment (https://github.com/akhilMotakatla/green-valley-hospital/pull/1#issuecomment-5032169696), then push the branch and request re-review.
- Plan: Read notification_service.py, lab.py, doctor.py, staff.py, NotificationPanel.tsx fully before editing. Fix Issues 1+4 together (both touch the same function), then Issue 2 (two files), then Issue 3 (string rename), then Issue 5 (one-liner). Verify py_compile + npm run build pass, then commit + push.

**During work**
- Read all 5 affected files before making any edit.
- Issue 1 (CRITICAL — data-loss): Restructured `check_and_fire_deferred_notifications` so the `pending_schedules` query is guarded inside `if patient:` and has an explicit `Appointment.patient_id == patient.patient_id` filter. This prevents the function from loading and marking-fired another patient's reminder schedule when the logged-in user is Patient A but the schedule belongs to Patient B.
- Issue 4 (MEDIUM — doctor reminder never fires): Added a second `create_notifications()` call inside the same loop, firing `appointment_reminder` to `appt.doctor.user_id` with a doctor-appropriate message body. Fires in the same loop iteration, before `sched.is_fired = 1`, so both patient and doctor are notified in a single pass without needing a separate query.
- Issue 2 (MEDIUM — NoShow fan-out missing): Added `elif payload.status == "NoShow":` block in `doctor.py` (after the Completed branch) and `elif payload.status == "NoShow" and prev_status != "NoShow":` block in `staff.py` (after the Cancelled branch). Both fire `appointment_noshow` to the patient's `user_id`.
- Issue 3 (MEDIUM — wrong event_type): Used replace_all to rename all 4 occurrences of `lab_result_finalized` to `lab_result_ready` in lab.py. Covers both the initial-result path (lines ~107, 116) and the amend path (lines ~183, 192).
- Issue 5 (MINOR — page_size): Changed `page_size: 10` to `page_size: 20` in NotificationPanel.tsx line 43.
- Verified: `python -m py_compile` on all 4 Python files passes. `npm run build` passes with zero TypeScript errors.

**After work completes**
- Files changed: `src/backend/app/services/notification_service.py`, `src/backend/app/routers/doctor.py`, `src/backend/app/routers/staff.py`, `src/backend/app/routers/lab.py`, `src/frontend/src/components/NotificationPanel.tsx`, `docs/project-status.md`, `communication/2026-07-20_00-00-00.md`.
- Commit: "fix: address Sagar's Phase 7 review issues (Issues 1–5)" pushed to `origin feature/chintu-group-a-foundation`.
- PR #1 is ready for Sagar's re-review. All 5 issues addressed. No new files added; all changes are targeted edits to previously-reviewed files.
- Handoff to Sagar: please re-review PR #1. Key items: (1) pending_schedules is now fully patient-scoped — verify the filter is in the right place relative to the `if patient:` guard. (2) Doctor reminder now fires from the patient's login context (not the doctor's) — this matches the OI-2 poll-on-login design where a single schedule row triggers both recipient notifications. (3) lab_result_ready replaces lab_result_finalized in all 4 notification inserts — no event_type strings were left unchanged. (4) NoShow is guarded with `prev_status != "NoShow"` in staff.py (idempotent) and left unguarded in doctor.py (the patch endpoint already relies on caller discipline; adding the guard to match staff.py would be a small improvement if desired).

